import { type } from 'os'
import React from 'react'

type Props = {
    id: string;
}

function ChatRow({id}: Props) {
    return (
        <div>
            chat row
        </div>
    )
}

export default ChatRow